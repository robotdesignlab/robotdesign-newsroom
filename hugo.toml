baseURL = "https://robotdesign.net/"
locale = "en"
title = "Robotdesign.net"

# 주소 끝에 항상 슬래시를 붙여 CloudFront-S3 경로 오작동을 차단합니다.
uglyURLs = false
canonifyURLs = true

[module]
  [[module.imports]]
    path = "github.com/nunocoracao/blowfish/v2"

[params]
  mainSections = ["posts"]

[params.homepage]
  layout = "background"
  showRecent = true
